import { ScoreRing } from '@/components/score-ring'
import { ResultCard } from '@/components/result-card'
import { reviewSections, productionReadinessScore } from '@/lib/review-data'
import { CheckCircle2, ShieldAlert, Wrench } from 'lucide-react'

const scoreStats = [
  { icon: CheckCircle2, label: 'Strengths', value: '5 areas', tint: 'text-[#3d7d00]' },
  { icon: ShieldAlert, label: 'Critical issues', value: '2 flagged', tint: 'text-destructive' },
  { icon: Wrench, label: 'Recommendations', value: '3 actions', tint: 'text-accent' },
]

export function ResultsSection({ projectName }: { projectName: string }) {
  return (
    <section id="results" className="mx-auto max-w-6xl px-4 py-16 sm:px-6">
      <div className="mb-10 flex flex-col items-start gap-2">
        <span className="inline-flex items-center gap-2 rounded-full bg-primary/15 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-[#9a5b00]">
          Review complete
        </span>
        <h2 className="text-3xl font-bold tracking-tight text-foreground text-balance">
          Architecture review
          {projectName ? <span className="text-muted-foreground"> · {projectName}</span> : null}
        </h2>
        <p className="max-w-2xl text-base leading-relaxed text-muted-foreground text-pretty">
          A structured, multi-dimensional assessment of your AWS architecture generated by ArchLens
          AI.
        </p>
      </div>

      {/* Production readiness score highlight */}
      <div className="mb-8 grid gap-6 rounded-3xl border border-border bg-card p-8 shadow-sm lg:grid-cols-[auto_1fr] lg:items-center">
        <div className="flex justify-center lg:justify-start">
          <ScoreRing score={productionReadinessScore} />
        </div>
        <div>
          <h3 className="text-xl font-semibold text-card-foreground">Production Readiness Score</h3>
          <p className="mt-2 max-w-xl text-sm leading-relaxed text-muted-foreground">
            Your architecture demonstrates strong serverless fundamentals and a solid security
            baseline. Addressing the flagged edge protection and data-tier resiliency items will
            push this workload to fully production-grade.
          </p>
          <dl className="mt-6 grid gap-4 sm:grid-cols-3">
            {scoreStats.map((stat) => {
              const Icon = stat.icon
              return (
                <div
                  key={stat.label}
                  className="rounded-2xl border border-border bg-secondary/60 p-4"
                >
                  <Icon className={`h-5 w-5 ${stat.tint}`} aria-hidden="true" />
                  <dt className="mt-2 text-xs font-medium uppercase tracking-wide text-muted-foreground">
                    {stat.label}
                  </dt>
                  <dd className="mt-0.5 text-lg font-semibold text-card-foreground">
                    {stat.value}
                  </dd>
                </div>
              )
            })}
          </dl>
        </div>
      </div>

      {/* Review cards grid */}
      <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        {reviewSections.map((section) => (
          <ResultCard key={section.id} section={section} />
        ))}
      </div>
    </section>
  )
}
